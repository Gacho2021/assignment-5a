// Set counter to 1
let scoreCount = -1;       
let scoreTotal = -999;
let entry;
// enter scores until 999 is entered
while (entry != 999) {
  entry = parseInt(prompt('Enter test score\nOr enter 999 to end entries'));
  let score1 = 40;
  let score2 = 50;
  let score3 = 60;
  scoreCount= 3;
  scoreTotal = 150
}
 
let Message= alert('Average score is ' + scoreTotal / scoreCount);
let firstP = document.getElementById(firstP)
firstP.textContent = message
